@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model List<CarRentalManagementSystem.Models.Booking>
@{
    ViewData["Title"] = "My Bookings";
}

<h2 class="mb-4">My Bookings</h2>

<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>Car</th>
            <th>Pickup Date</th>
            <th>Return Date</th>
            <th>Total Cost (LKR)</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var booking in Model)
        {
            <tr>
                <td>@booking.Car.CarName (@booking.Car.CarModel)</td>
                <td>@booking.PickupDate.ToShortDateString()</td>
                <td>@booking.ReturnDate.ToShortDateString()</td>
                <td>@String.Format("{0:N0}", booking.TotalCost)</td>
                <td>
                    <form asp-action="Cancel" method="post">
                        <input type="hidden" name="id" value="@booking.BookingID" />
                        <button type="submit" class="btn btn-danger btn-sm">Cancel</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>

